<!DOCTYPE html>
<html lang="en">
  <%- include ('head') %>
  <body>
    <%- include ('header') %>
    <div class="genre-main">
      <h2 class="genre-name"><%= genre.name %></h2>
      <div class="genre-books">
        <% if (genreBooks.length) {%> <% genreBooks.forEach( (book) => { %>
        <p>
          <a href="<%= book.url %>"> <%= book.title %> </a>
        </p>
        <%} ) %> <% } else { %>
        <p>There are no genres, try add one <a href="/genre/add">here</a></p>
        <% } %>
      </div>
    </div>
    <p class="update">
      <a href="<%=genre.url%>/update">Update Book</a>
    </p>
    <p class="delete">
      <a href="<%=genre.url%>/delete">Delete Book</a>
    </p>
    <%- include ('footer') %>
  </body>
</html>
