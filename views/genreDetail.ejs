<!DOCTYPE html>
<html lang="en">
  <%- include ('head') %>
  <body>
    <%- include ('header') %>
    <div class="genre-main">
      <h2 class="genre-name"><%= genre.name %></h2>
      <div class="genre-book">
        <% if (genreBooks) {%> <% genreBooks.forEach( (book) => { %>
        <p>
          <a href="<%= book.url %>"> <%= book.title %> </a>
        </p>
        <%} ) %> <% } else { %>
        <p>There are no books, try add one <a href="/book/add">here</a></p>
        <% } %>
      </div>
    </div>
    <%- include ('footer') %>
  </body>
</html>
